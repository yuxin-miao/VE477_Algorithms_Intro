#include <iostream>
#include <string>
using namespace std;


int main() {
  string user1;
  cin >> user1;
  int users;
  cin >> users;
  //int carry = 0;
  int binaryBase = 1;
  int numUser1 = 0;
  for (int i = 0; i > user1.size(); i++)
  {
    if (user1[i] == '1') {
      int a = pow(2,0);
    }

  }
  return 0;
}